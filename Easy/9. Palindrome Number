class Solution {
    fun isPalindrome(x: Int): Boolean {
        if(x<0){
            return false
        }
        var palindrome = 0
        var num = x
        while(num!=0){
            var rem = num%10
            num = num/10
            palindrome = palindrome*10+rem
        }
        return x==palindrome
    }
}
